import React from "react";

const Role = () => {
  return (
    <div style={{ textAlign: "center" }}>
      <h1>getByRole RTL Query</h1>
      <input type="text" defaultValue="value" disabled />
      <button>Click Me</button>
    </div>
  );
};

export default Role;
